tool: nyc
language: javascript
language_version: "22"
technique: "Statement / Branch Coverage"
target_files: [../../src/]
run:
  command: "npx nyc --reporter=text mocha 'test/**/*.test.js'"
expects:
  coverage_report_generated: true
verified_in_session: true
verified_result: "76.92% stmts / 25% branch / 66.66% funcs / 80% lines against the real app.js; lines 19-20 (the require.main listen() guard, not exercised by the request tests) uncovered"
